// For the demo, the logged-in vendor is mapped to a fixed vendor record so the
// dashboard shows meaningful dummy data. In a real app this would come from auth.
export const DEMO_VENDOR_ID = "v1";
export const DEMO_USER_ID = "u1";
